CRUD
Create, Rename, Update, Delete

NULL = NULL (Any operations performed will result in NULL)

IS Operator 
Filter values that are NULL, NOT NULL, True, False
-- SELECT * FROM <table> WHERE <field> IS [NOT] NULL;
